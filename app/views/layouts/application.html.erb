<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <title><%= content_for?(:title) ? yield(:title) : "Learning App" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-slate-50">
    <!-- ヘッダー -->
    <header class="fixed top-0 left-0 w-full bg-white shadow-md z-10">
      <div class="container mx-auto px-5 py-4 flex justify-between items-center">
        <%= link_to "ReCall", root_path, class: "text-xl font-bold text-gray-800" %>

        <nav class="flex items-center gap-4">
          <% if user_signed_in? %>
            <span class="text-slate-700">ログイン中: <%= current_user.name %></span>
            <%= link_to 'ログアウト', destroy_user_session_path, data: { turbo_method: :delete },
                        class: "px-4 py-2 rounded border border-slate-300 text-slate-700 hover:bg-slate-100 transition" %>
          <% else %>
            <%= link_to "新規登録", new_user_registration_path,
                        class: "px-4 py-2 rounded bg-emerald-500 text-white hover:bg-emerald-600 transition" %>
            <%= link_to "ログイン", new_user_session_path,
                        class: "px-4 py-2 rounded border border-slate-300 text-slate-700 hover:bg-slate-100 transition" %>
          <% end %>
        </nav>
      </div>
    </header>

    <!-- ページ固有コンテンツ -->
    <main class="container mx-auto mt-28 px-5">
      <%= yield %>
    </main>
  </body>
</html>
